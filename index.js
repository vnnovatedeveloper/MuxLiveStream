/**
 * @format
 */

import {AppRegistry} from 'react-native';
import ViewStream from './ViewStream';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ViewStream);
